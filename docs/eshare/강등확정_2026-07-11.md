# eShare 사이드 강등 확정 기록 (게이트2 발화)

> 이 문서가 강등 상태의 **정본**이다. (`.omc/plans/`의 계획 문서들은 gitignore된 비영속 미러)

## 확정 사실 (2026-07-11 금)

- **게이트2 발화**: 계획 C′ §3 "인증키 ~7/11 미도착 시 자동 사이드 강등" 조건 충족.
- 공유누리 신청내역 페이지 직접 확인(로그인 후): 신청 6건 **전부 '승인 대기'** — 반려/보완요청 아님. 증빙: [신청내역_승인대기_2026-07-11.png](신청내역_승인대기_2026-07-11.png)
- API 프로브 401(pending) 일치. 신청 계정 메일(ynr0402@gmail.com)에도 통보 없음.
- 신청일 7/4, 안내 검토기간 3~5일의 2배 경과(7일째).

## 강등의 의미

- eShare에 **메인 시간 투입 정지**. 목업 기반 구현·QA는 완성 상태로 동결(잃는 것 없음).
- 승인 감지는 자동화(아래)에 위임 — 사람이 확인하러 들어가지 않는다.

## 복귀 조건

- **승인 자동 감지 시 당일** `eshare_mcp/docs/승인후_잔여작업.md` 체크리스트로 복귀(씬슬라이스 빌드 → 파서 조정 → KC 2호기 배포 → 심사요청).
- **컷오프: PlayMCP 제출 마감 2026-07-15(화).** 화요일 오후까지 승인 없으면 이번 회차 완전 종료. 승인 감지 자체는 이후에도 유효(차기 회차용 라이브 전환 가능).
- 심사 마감·기존작(배움알리미, 7/7 심사요청 완료) 우선순위와 충돌 시 복귀 보류.

## 승인 자동 감지 (감시 장치)

- 스크립트: `eshare_mcp/devtools/watch_approval.sh` — `/opt/homebrew/bin/python3`(3.14) 고정, `scripts/build_db.py --probe-only` 1콜, stdout JSON `approved` 파싱.
- 크론: 상시 하루 2회(10·18시) + 7/14~15 업무시간 매시간(9:30~18:30).
- 관측 정본 채널 = `eshare_mcp/devtools/watch_approval.log` (매 실행 기록). macOS 알림은 best-effort.
- 승인 감지 시 `devtools/.approved` 센티넬 생성 → 이후 실행 no-op(중복 알림 방지). 재무장은 파일 삭제.

## 지연 문의

- 2026-07-11 공유누리에 승인 지연 문의 제출 (접수 증빙은 본 폴더에 보관). 상태: _제출 후 갱신_
