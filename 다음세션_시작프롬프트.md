# 다음 세션 시작 프롬프트 (복붙용)

아래 블록을 새 세션 첫 메시지로 붙여넣으세요.

---

카카오 AGENTIC PLAYER 10 공모전 출품작 개발을 시작한다. 계획 수립과 데이터 검증은 이전 세션에서 완료됐고 사용자 승인도 끝났다.

필수 선행 읽기 (순서대로):
1. `실행명세_PRD.md` — 태스크 분해·tool 스펙·데이터 접근 레시피·AC 전부 여기 있음
2. `공모전_MCP_요구사항_분석.md` — 룰북 (심사 반려 사유 §4 특히)
3. `.omc/plans/agentic-player10-lifelong-mcp-plan.md` — 합의 계획 + 7/2 실측 결과

핵심 컨텍스트:
- 마감: **7/7(월) PlayMCP 심사 요청** 이 절대 마지노선 (오늘 날짜 확인 후 역산할 것)
- 데이터 검증 완료: `data/파일카탈로그_uddi목록.json`(205개 파일 uddi), `data/전수조사결과_20260702.json`, `data/강남구_평생학습강좌_샘플.csv` — 다운로드 레시피는 PRD §5에 실증된 그대로 있음, 재발명 금지
- 아키텍처 확정: 배치 수집 → SQLite(레포 커밋) → Dockerfile COPY → FastMCP(Python, Streamable HTTP, stateless) → PlayMCP in KC 배포. hot-path 외부호출 0
- Tool 6개 확정 (PRD §3): search_courses, get_enrollment_calendar(스타 기능), compare_courses, get_course_detail, list_courses_by_center, get_filter_options
- 하지 말 것: 강남구 숨은 API 사용, "kakao" 명칭, raw 응답, 확정형 예측 표현 (PRD §9)

/ralph PRD §6 태스크 1번(수집 파이프라인)부터 순서대로 실행하라. 각 태스크는 PRD §7의 AC로 검증하고, AC 미충족이면 다음으로 넘어가지 마라. 사용자 작업(PRD §8 — 회원가입·API키·캡처·서비스명)이 블로커가 되면 명확히 요청하고 그동안 다른 태스크를 진행하라.

---

## 시작 전 사용자가 미리 해두면 좋은 것 (PRD §8)
- [ ] PlayMCP 회원가입 확인 + PlayMCP in KC(playmcp.kakaocloud.io) 접속 확인
- [ ] GitHub 레포 생성 (public 권장)
- [ ] 서비스명 후보 생각해오기 ("kakao" 금지)
- [ ] (선택) 서울 열린데이터광장 API 키 발급
